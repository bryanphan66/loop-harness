# Dev-session local config for working ON this repo (Claude Code convention).
# NOT the shipped product config — that is harness/.claude/ (installed into projects).
.claude/worktrees/
.claude/settings.local.json
.claude/agent-memory/

# OS / editor cruft
.DS_Store
*.swp
