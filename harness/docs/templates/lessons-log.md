<!-- Template: docs/lessons-log.md — one of the 4 knowledge stores. Each PROJECT keeps
     its own. Copied into the project at bootstrap; the control session appends a row
     every time it gets bitten. Newest on top. Not the loop-harness dev lessons (those
     live in the harness workshop). -->
# Sổ tay bài học & sai lầm (lessons-log)

Bồi đắp mỗi lần vấp. Mỗi mục: **triệu chứng -> nguyên nhân gốc -> luật rút ra**. Mới nhất trên cùng.

## YYYY-MM-DD

**L1 - <tiêu đề ngắn triệu chứng>.**
<mô tả điều đã xảy ra + vì sao review/check trước đó không bắt được>
-> Luật: <luật self-contained, giải thích cái WHY, không tham chiếu mã plan/finding>.
